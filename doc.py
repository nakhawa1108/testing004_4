def a():
    '''    git status checks the status.
    git add adds the file to the temporary file storage.
    git commit stores file to the local storage.
    git push origin main pushes file to the remote github.'''
print(a.__doc__)